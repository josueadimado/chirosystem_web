import { Loader } from "@/components/loader";

/** Shown during navigations to the home/booking route while the page shell loads. */
export default function RootLoading() {
  return <Loader variant="screen" />;
}
